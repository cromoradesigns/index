<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Updated Title -->
    <title>Cromora Designs</title>
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;700&family=Space+Grotesk:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            /* Palette */
            --bg-body: #f8fafc;
            --bg-card: #ffffff;
            --bg-secondary: #f1f5f9;
            
            --primary: #4f46e5;
            --primary-dark: #3730a3;
            --primary-light: #e0e7ff;
            
            --accent: #06b6d4;
            --accent-light: #cffafe;
            
            --text-main: #0f172a;
            --text-body: #334155;
            --text-muted: #64748b;
            
            --border: #e2e8f0;
            --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
            --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1);
            --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1);
            --shadow-xl: 0 20px 25px -5px rgb(0 0 0 / 0.1);

            --font-heading: 'Space Grotesk', sans-serif;
            --font-body: 'Outfit', sans-serif;
            --ease-out: cubic-bezier(0.215, 0.61, 0.355, 1);
        }

        * { margin: 0; padding: 0; box-sizing: border-box; cursor: none; /* Hide default cursor for custom one */ }

        body {
            background-color: var(--bg-body);
            color: var(--text-body);
            font-family: var(--font-body);
            overflow-x: hidden;
            line-height: 1.7;
            /* Dot Pattern */
            background-image: radial-gradient(#cbd5e1 1px, transparent 1px);
            background-size: 20px 20px;
        }

        /* --- Custom Cursor --- */
        .cursor-dot, .cursor-outline {
            position: fixed;
            top: 0; left: 0;
            transform: translate(-50%, -50%);
            border-radius: 50%;
            z-index: 9999;
            pointer-events: none;
        }
        .cursor-dot {
            width: 8px; height: 8px;
            background-color: var(--primary);
        }
        .cursor-outline {
            width: 40px; height: 40px;
            border: 1px solid var(--primary);
            transition: width 0.2s, height 0.2s, background-color 0.2s;
        }
        body:hover .cursor-outline.hovered {
            width: 60px; height: 60px;
            background-color: rgba(79, 70, 229, 0.1);
            border-color: transparent;
        }

        /* --- Utilities --- */
        a { text-decoration: none; color: inherit; }
        ul { list-style: none; }
        .container { max-width: 1280px; margin: 0 auto; padding: 0 24px; }
        .section-padding { padding: 120px 0; position: relative; }
        
        .gradient-text {
            background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            gap: 12px;
            padding: 16px 36px;
            border-radius: 100px;
            font-weight: 600;
            font-size: 1rem;
            transition: transform 0.3s var(--ease-out), box-shadow 0.3s;
            position: relative;
            overflow: hidden;
            z-index: 1;
        }

        .btn-primary {
            background: var(--text-main);
            color: #fff;
        }
        .btn-primary::before {
            content: '';
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            background: linear-gradient(90deg, var(--primary), var(--accent));
            z-index: -1;
            transition: opacity 0.3s;
            opacity: 0;
        }
        .btn-primary:hover::before { opacity: 1; }
        .btn-primary:hover { transform: translateY(-3px); box-shadow: 0 10px 20px rgba(79, 70, 229, 0.3); }

        .btn-outline {
            border: 1px solid var(--border);
            background: #fff;
            color: var(--text-main);
        }
        .btn-outline:hover {
            border-color: var(--text-main);
            background: var(--text-main);
            color: #fff;
        }

        /* --- Progress Bar --- */
        .progress-bar {
            position: fixed;
            top: 0; left: 0;
            height: 3px;
            background: linear-gradient(90deg, var(--primary), var(--accent));
            width: 0%;
            z-index: 10000;
            transition: width 0.1s;
        }

        /* --- Header --- */
        header {
            position: fixed;
            top: 0; width: 100%;
            z-index: 1000;
            padding: 20px 0;
            transition: 0.4s var(--ease-out);
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid transparent;
        }
        header.scrolled { padding: 15px 0; border-bottom: 1px solid var(--border); }

        .nav-content { display: flex; justify-content: space-between; align-items: center; }
        .logo { font-family: var(--font-heading); font-weight: 700; font-size: 1.5rem; display: flex; align-items: center; gap: 10px; }
        .logo i { color: var(--primary); animation: rotateLogo 10s linear infinite; }
        
        .nav-links { display: flex; gap: 40px; }
        .nav-link {
            font-size: 0.95rem;
            font-weight: 500;
            position: relative;
            color: var(--text-body);
        }
        .nav-link::after {
            content: ''; position: absolute; bottom: -5px; left: 0; width: 0; height: 2px;
            background: var(--primary); transition: width 0.3s var(--ease-out);
        }
        .nav-link:hover::after, .nav-link.active::after { width: 100%; }

        .mobile-toggle { display: none; font-size: 1.5rem; }

        /* --- Canvas --- */
        #canvas-bg { position: fixed; top: 0; left: 0; width: 100%; height: 100%; z-index: -1; opacity: 0.5; }

        /* --- Page Structure --- */
        main { padding-top: 80px; min-height: 100vh; }
        .page-section { display: none; opacity: 0; animation: fadeIn 0.8s forwards; }
        .page-section.active-page { display: block; }

        @keyframes fadeIn { to { opacity: 1; } }
        @keyframes rotateLogo { 100% { transform: rotate(360deg); } }
        @keyframes float { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-20px); } }

        /* --- Scroll Animation Classes --- */
        .reveal {
            opacity: 0; transform: translateY(40px);
            transition: all 1s var(--ease-out);
        }
        .reveal.active { opacity: 1; transform: translateY(0); }
        .reveal-delay-1 { transition-delay: 0.1s; }
        .reveal-delay-2 { transition-delay: 0.2s; }
        .reveal-delay-3 { transition-delay: 0.3s; }

        /* --- HOME PAGE --- */
        .hero { min-height: calc(100vh - 80px); display: flex; align-items: center; position: relative; overflow: hidden; }
        
        /* Floating Shapes in Hero */
        .shape {
            position: absolute;
            border-radius: 50%;
            filter: blur(60px);
            z-index: -1;
            opacity: 0.4;
            animation: float 6s ease-in-out infinite;
        }
        .shape-1 { width: 300px; height: 300px; background: var(--primary-light); top: -50px; right: -50px; }
        .shape-2 { width: 400px; height: 400px; background: var(--accent-light); bottom: -100px; left: -100px; animation-delay: 2s; }

        .hero h1 {
            font-family: var(--font-heading); font-size: 5rem; line-height: 1.05;
            color: var(--text-main); margin-bottom: 30px; letter-spacing: -2px;
        }
        
        .hero p {
            font-size: 1.35rem; color: var(--text-muted); max-width: 600px; margin-bottom: 40px;
        }

        .scroll-indicator {
            position: absolute; bottom: 40px; left: 50%; transform: translateX(-50%);
            animation: bounce 2s infinite;
        }
        @keyframes bounce { 0%, 20%, 50%, 80%, 100% {transform: translateX(-50%) translateY(0);} 40% {transform: translateX(-50%) translateY(-10px);} 60% {transform: translateX(-50%) translateY(-5px);} }

        /* Marquee */
        .marquee-container {
            width: 100%; overflow: hidden; background: var(--text-main); color: #fff;
            padding: 20px 0; margin: 80px 0; transform: rotate(-2deg) scale(1.05);
        }
        .marquee-content { display: flex; white-space: nowrap; animation: marquee 20s linear infinite; }
        .marquee-item {
            font-family: var(--font-heading); font-size: 2.5rem; font-weight: 700;
            margin: 0 40px; opacity: 0.8; text-transform: uppercase;
        }
        @keyframes marquee { 0% { transform: translateX(0); } 100% { transform: translateX(-50%); } }

        /* Stats & Testimonials */
        .stats-grid {
            display: grid; grid-template-columns: repeat(4, 1fr); gap: 30px;
            background: #fff; padding: 50px; border-radius: 24px;
            box-shadow: var(--shadow-xl); border: 1px solid var(--border);
        }
        .stat-item h3 { font-size: 3rem; font-family: var(--font-heading); color: var(--primary); }
        .stat-item p { color: var(--text-muted); font-weight: 500; }

        .testimonial-grid {
            display: grid; grid-template-columns: repeat(3, 1fr); gap: 30px; margin-top: 80px;
        }
        .testimonial-card {
            background: #fff; padding: 40px; border-radius: 20px; border: 1px solid var(--border);
            position: relative; transition: transform 0.3s;
        }
        .testimonial-card:hover { transform: translateY(-10px); box-shadow: var(--shadow-lg); }
        .quote-icon { font-size: 2rem; color: var(--accent-light); margin-bottom: 20px; }
        .client-info { display: flex; align-items: center; gap: 15px; margin-top: 25px; }
        .client-avatar { width: 50px; height: 50px; border-radius: 50%; object-fit: cover; }

        /* --- ABOUT PAGE --- */
        .about-hero { text-align: center; max-width: 800px; margin: 0 auto 80px; }
        .about-hero h1 { font-family: var(--font-heading); font-size: 3.5rem; margin-bottom: 20px; }

        .timeline {
            position: relative; max-width: 900px; margin: 100px auto;
            padding-left: 50px;
        }
        .timeline::before {
            content: ''; position: absolute; left: 20px; top: 0; bottom: 0; width: 2px; background: var(--border);
        }
        .timeline-item { position: relative; margin-bottom: 60px; }
        .timeline-dot {
            position: absolute; left: -39px; top: 5px; width: 16px; height: 16px;
            background: var(--primary); border-radius: 50%; border: 4px solid #fff;
            box-shadow: 0 0 0 2px var(--border);
        }
        .timeline-date { color: var(--accent); font-weight: 600; margin-bottom: 5px; display: block; }
        
        .team-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 40px; }
        .team-card {
            background: #fff; border-radius: 20px; overflow: hidden; border: 1px solid var(--border);
            group: team; transition: transform 0.3s;
        }
        .team-card:hover { transform: translateY(-10px); }
        .team-img-wrapper { height: 300px; overflow: hidden; position: relative; }
        .team-img-wrapper img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.5s; }
        .team-card:hover .team-img-wrapper img { transform: scale(1.1); }
        .team-social {
            position: absolute; bottom: -50px; left: 0; width: 100%;
            background: rgba(255,255,255,0.9); display: flex; justify-content: center; gap: 15px;
            padding: 15px 0; transition: bottom 0.3s;
        }
        .team-card:hover .team-social { bottom: 0; }
        .team-info { padding: 25px; text-align: center; }

        /* --- SERVICES PAGE --- */
        .service-card {
            background: #fff; padding: 50px 40px; border-radius: 24px; border: 1px solid var(--border);
            transition: all 0.4s var(--ease-out); position: relative; overflow: hidden;
        }
        .service-card:hover {
            border-color: var(--primary); box-shadow: var(--shadow-xl); transform: translateY(-5px);
        }
        .service-icon {
            width: 70px; height: 70px; background: var(--bg-secondary); border-radius: 16px;
            display: flex; align-items: center; justify-content: center; font-size: 1.8rem;
            color: var(--primary); margin-bottom: 30px; transition: 0.4s;
        }
        .service-card:hover .service-icon { background: var(--primary); color: #fff; transform: rotateY(180deg); }
        .service-list { margin-top: 25px; border-top: 1px solid var(--border); padding-top: 25px; }
        .service-list li { margin-bottom: 10px; display: flex; gap: 10px; color: var(--text-muted); font-size: 0.95rem; }
        .service-list li i { color: var(--accent); margin-top: 4px; }

        /* --- PORTFOLIO PAGE --- */
        .portfolio-filter {
            display: flex; justify-content: center; gap: 20px; margin-bottom: 50px; flex-wrap: wrap;
        }
        .filter-btn {
            background: none; border: none; font-size: 1rem; font-weight: 500;
            color: var(--text-muted); padding: 8px 16px; border-radius: 20px; transition: 0.3s;
        }
        .filter-btn.active, .filter-btn:hover { background: var(--text-main); color: #fff; }

        .portfolio-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(400px, 1fr)); gap: 40px; }
        .project-card {
            border-radius: 24px; overflow: hidden; position: relative; height: 500px; group: project;
        }
        .project-img {
            width: 100%; height: 100%; object-fit: cover; transition: transform 0.6s cubic-bezier(0.25, 1, 0.5, 1);
        }
        .project-card:hover .project-img { transform: scale(1.1); }
        
        .project-overlay {
            position: absolute; inset: 0; background: linear-gradient(to top, rgba(15, 23, 42, 0.9), transparent);
            display: flex; flex-direction: column; justify-content: flex-end; padding: 40px;
            opacity: 0; transform: translateY(20px); transition: all 0.4s;
        }
        .project-card:hover .project-overlay { opacity: 1; transform: translateY(0); }
        
        .project-number {
            position: absolute; top: 30px; right: 30px; font-family: var(--font-heading);
            font-size: 4rem; color: rgba(255,255,255,0.1); font-weight: 700; line-height: 1;
        }

        /* --- PROCESS PAGE --- */
        .process-steps { display: flex; flex-direction: column; gap: 100px; margin-top: 80px; }
        .step { display: flex; align-items: center; gap: 60px; }
        .step:nth-child(even) { flex-direction: row-reverse; }
        
        .step-img {
            flex: 1; height: 400px; border-radius: 24px; overflow: hidden;
            box-shadow: var(--shadow-lg); position: relative;
        }
        .step-img img { width: 100%; height: 100%; object-fit: cover; }
        .step-content { flex: 1; }
        
        .step-number {
            font-size: 4rem; font-family: var(--font-heading); color: var(--bg-secondary);
            font-weight: 700; line-height: 1; margin-bottom: 20px; -webkit-text-stroke: 2px var(--border);
        }

        /* --- CONTACT PAGE --- */
        .contact-layout {
            display: grid; grid-template-columns: 1fr 1.5fr; gap: 60px; background: #fff;
            border-radius: 30px; overflow: hidden; box-shadow: var(--shadow-xl);
        }
        .contact-info {
            background: var(--text-main); color: #fff; padding: 80px 60px; position: relative;
        }
        .contact-info::before {
            content: ''; position: absolute; top: 0; left: 0; width: 100%; height: 100%;
            background: url('https://www.transparenttextures.com/patterns/cubes.png'); opacity: 0.1;
        }
        
        .contact-form { padding: 80px 60px; }
        .form-group { position: relative; margin-bottom: 30px; }
        .form-control {
            width: 100%; background: #f8fafc; border: 2px solid transparent;
            padding: 20px; border-radius: 12px; font-size: 1rem; font-family: var(--font-body);
            transition: 0.3s;
        }
        .form-control:focus { outline: none; border-color: var(--primary); background: #fff; }
        .form-label {
            position: absolute; left: 20px; top: 20px; color: var(--text-muted);
            pointer-events: none; transition: 0.3s;
        }
        .form-control:focus ~ .form-label, .form-control:not(:placeholder-shown) ~ .form-label {
            top: -10px; left: 15px; font-size: 0.8rem; background: #fff; padding: 0 5px; color: var(--primary);
        }

        /* --- Toast --- */
        .toast {
            position: fixed; bottom: 40px; right: 40px; background: #fff; padding: 20px 30px;
            border-radius: 12px; box-shadow: var(--shadow-xl); border-left: 5px solid var(--primary);
            display: flex; align-items: center; gap: 15px; transform: translateX(200%); transition: 0.5s cubic-bezier(0.68, -0.55, 0.27, 1.55); z-index: 9999;
        }
        .toast.show { transform: translateX(0); }

        /* --- Responsive --- */
        @media (max-width: 992px) {
            .hero h1 { font-size: 3.5rem; }
            .stats-grid, .testimonial-grid, .contact-layout { grid-template-columns: 1fr; }
            .step { flex-direction: column !important; gap: 30px; }
            .step-img { width: 100%; height: 300px; }
        }
        @media (max-width: 768px) {
            .nav-links {
                position: fixed; top: 0; right: 0; height: 100vh; width: 80%;
                background: #fff; flex-direction: column; justify-content: center; align-items: center;
                transform: translateX(100%); transition: 0.4s var(--ease-out); box-shadow: -10px 0 30px rgba(0,0,0,0.1);
            }
            .nav-links.active { transform: translateX(0); }
            .mobile-toggle { display: block; z-index: 1001; }
            .cursor-dot, .cursor-outline { display: none; } /* Disable custom cursor on touch */
            * { cursor: auto; }
            .hero h1 { font-size: 2.5rem; }
        }
    </style>

    <style>
        :root {
            /* Palette */
            --bg-body: #f8fafc;
            --bg-card: #ffffff;
            --bg-secondary: #f1f5f9;
            
            --primary: #4f46e5;
            --primary-dark: #3730a3;
            --primary-light: #e0e7ff;
            
            --accent: #06b6d4;
            --accent-light: #cffafe;
            
            --text-main: #0f172a;
            --text-body: #334155;
            --text-muted: #64748b;
            
            --border: #e2e8f0;
            --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
            --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1);
            --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1);
            --shadow-xl: 0 20px 25px -5px rgb(0 0 0 / 0.1);

            --font-heading: 'Space Grotesk', sans-serif;
            --font-body: 'Outfit', sans-serif;
            --ease-out: cubic-bezier(0.215, 0.61, 0.355, 1);
            --ease-elastic: cubic-bezier(0.68, -0.55, 0.265, 1.55);
        }

        * { margin: 0; padding: 0; box-sizing: border-box; cursor: auto; }

        body {
            background-color: var(--bg-body);
            color: var(--text-body);
            font-family: var(--font-body);
            overflow-x: hidden;
            line-height: 1.7;
            background-image: radial-gradient(#cbd5e1 1px, transparent 1px);
            background-size: 20px 20px;
        }

        /* --- Custom Cursor --- */
        .cursor-dot, .cursor-outline {
            position: fixed; top: 0; left: 0; transform: translate(-50%, -50%);
            border-radius: 50%; z-index: 9999; pointer-events: none;
        }
        .cursor-dot { width: 8px; height: 8px; background-color: var(--primary); transition: transform 0.1s; }
        .cursor-outline {
            width: 40px; height: 40px; border: 1px solid var(--primary);
            transition: width 0.3s, height 0.3s, background-color 0.3s, transform 0.15s ease-out;
        }
        body:hover .cursor-outline.hovered {
            width: 60px; height: 60px;
            background-color: rgba(79, 70, 229, 0.1); border-color: transparent;
        }

        /* --- Utilities --- */
        a { text-decoration: none; color: inherit; }
        ul { list-style: none; }
        .container { max-width: 1280px; margin: 0 auto; padding: 0 24px; }
        .section-padding { padding: 100px 0; position: relative; }
        
        .gradient-text {
            background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
            -webkit-background-clip: text; -webkit-text-fill-color: transparent;
        }

        /* --- Magnetic & 3D Tilt Styles --- */
        .btn {
            display: inline-flex; align-items: center; justify-content: center;
            padding: 16px 36px; border-radius: 100px; font-weight: 600; font-size: 1rem;
            transition: transform 0.1s linear; /* Fast transition for magnetic */
            position: relative; overflow: hidden; z-index: 1;
        }
        .btn-primary {
            background: var(--text-main); color: #fff;
        }
        .btn-primary::before {
            content: ''; position: absolute; top: 0; left: 0; width: 100%; height: 100%;
            background: linear-gradient(90deg, var(--primary), var(--accent)); z-index: -1;
            transition: opacity 0.3s; opacity: 0;
        }
        .btn-primary:hover::before { opacity: 1; }
        .btn-primary:hover { box-shadow: 0 10px 20px rgba(79, 70, 229, 0.3); }

        .btn-outline {
            border: 1px solid var(--border); background: #fff; color: var(--text-main);
        }
        .btn-outline:hover { border-color: var(--text-main); background: var(--text-main); color: #fff; }
        .btn-white { background: #fff; color: var(--text-main); }
        .btn-white:hover { background: var(--primary); color: #fff; }

        /* 3D Tilt Card Base */
        .tilt-card {
            transform-style: preserve-3d;
            transform: perspective(1000px);
            transition: transform 0.1s ease-out; /* Smooth follow */
        }
        .tilt-inner { transform: translateZ(20px); }

        /* --- Text Animation --- */
        .split-text span {
            display: inline-block;
            opacity: 0;
            transform: translateY(50px) rotate(5deg);
            transition: all 0.8s var(--ease-out);
        }
        .split-text.visible span {
            opacity: 1;
            transform: translateY(0) rotate(0);
        }

        /* --- Progress Bar --- */
        .progress-bar {
            position: fixed; top: 0; left: 0; height: 3px;
            background: linear-gradient(90deg, var(--primary), var(--accent));
            width: 0%; z-index: 10000; transition: width 0.1s;
        }

        /* --- Header --- */
        header {
            position: fixed; top: 0; width: 100%; z-index: 1000; padding: 20px 0;
            transition: 0.4s var(--ease-out); background: rgba(255, 255, 255, 0.8);
            backdrop-filter: blur(20px); border-bottom: 1px solid transparent;
        }
        header.scrolled { padding: 15px 0; border-bottom: 1px solid var(--border); }
        .nav-content { display: flex; justify-content: space-between; align-items: center; }
        .logo { 
            font-family: var(--font-heading); font-weight: 700; font-size: 1.5rem; 
            display: flex; align-items: center; gap: 10px; 
        }
        
        /* Logo Image & Animation */
        .logo-img {
            width: 45px; height: 45px;
            border-radius: 10px; /* Slight rounded corners for modern look */
            object-fit: cover;
            /* Animation applied to image instead of icon */
            animation: rotateLogo 10s linear infinite;
        }

        .nav-links { display: flex; gap: 40px; }
        .nav-link { font-size: 0.95rem; font-weight: 500; position: relative; color: var(--text-body); transition: color 0.3s; }
        .nav-link::after {
            content: ''; position: absolute; bottom: -5px; left: 0; width: 0; height: 2px;
            background: var(--primary); transition: width 0.3s var(--ease-out);
        }
        .nav-link:hover::after, .nav-link.active::after { width: 100%; }
        .mobile-toggle { display: none; font-size: 1.5rem; }

        /* --- Canvas --- */
        #canvas-bg { position: fixed; top: 0; left: 0; width: 100%; height: 100%; z-index: -1; opacity: 0.5; }

        /* --- Page Structure --- */
        main { padding-top: 80px; min-height: 100vh; }
        .page-section { display: none; opacity: 0; animation: fadeIn 0.8s forwards; }
        .page-section.active-page { display: block; }

        @keyframes fadeIn { to { opacity: 1; } }
        @keyframes rotateLogo { 100% { transform: rotate(360deg); } }
        
        /* --- Scroll Animation Classes --- */
        .reveal {
            opacity: 0; transform: translateY(40px);
            transition: all 1s var(--ease-out);
        }
        .reveal.active { opacity: 1; transform: translateY(0); }
        
        /* Stagger delays */
        .reveal:nth-child(1) { transition-delay: 0ms; }
        .reveal:nth-child(2) { transition-delay: 100ms; }
        .reveal:nth-child(3) { transition-delay: 200ms; }
        .reveal:nth-child(4) { transition-delay: 300ms; }
        .reveal:nth-child(5) { transition-delay: 400ms; }
        .reveal:nth-child(6) { transition-delay: 500ms; }

        /* --- HOME PAGE (With Video) --- */
        .hero { 
            min-height: calc(100vh - 80px); 
            display: flex; align-items: center; 
            position: relative; 
            overflow: hidden; 
            perspective: 1000px; 
        }

        /* Video Background Styles */
        .hero-video-wrapper {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            z-index: -5; /* Behind everything */
            overflow: hidden;
        }
        .hero-video-wrapper video {
            width: 100%; height: 100%;
            object-fit: cover;
        }
        /* Light Theme Overlay to ensure text readability */
        .hero-overlay {
            position: absolute;
            inset: 0;
            background: rgba(255, 255, 255, 0.75); /* White wash */
            backdrop-filter: blur(2px);
            z-index: -4;
        }
        
        /* Parallax Shapes (Still present, overlaying the video) */
        .shape {
            position: absolute; border-radius: 50%; filter: blur(60px); z-index: -1; opacity: 0.6;
            will-change: transform;
        }
        .shape-1 { width: 300px; height: 300px; background: var(--primary-light); top: -50px; right: -50px; }
        .shape-2 { width: 400px; height: 400px; background: var(--accent-light); bottom: -100px; left: -100px; }
        
        .hero h1 {
            font-family: var(--font-heading); font-size: 5rem; line-height: 1.05;
            color: var(--text-main); margin-bottom: 30px; letter-spacing: -2px;
        }
        .hero p { font-size: 1.35rem; color: var(--text-muted); max-width: 600px; margin-bottom: 40px; }
        
        .marquee-container {
            width: 100%; overflow: hidden; background: var(--text-main); color: #fff;
            padding: 20px 0; margin: 80px 0; transform: rotate(-2deg) scale(1.05);
        }
        .marquee-content { display: flex; white-space: nowrap; animation: marquee 20s linear infinite; }
        .marquee-item { font-family: var(--font-heading); font-size: 2.5rem; font-weight: 700; margin: 0 40px; opacity: 0.8; text-transform: uppercase; }
        @keyframes marquee { 0% { transform: translateX(0); } 100% { transform: translateX(-50%); } }

        .stats-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 30px; background: #fff; padding: 50px; border-radius: 24px; box-shadow: var(--shadow-xl); border: 1px solid var(--border); }
        .stat-item h3 { font-size: 3rem; font-family: var(--font-heading); color: var(--primary); }
        .stat-item p { color: var(--text-muted); font-weight: 500; }

        .testimonial-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 30px; margin-top: 80px; }
        .testimonial-card { background: #fff; padding: 40px; border-radius: 20px; border: 1px solid var(--border); transition: box-shadow 0.3s; }
        .testimonial-card:hover { box-shadow: var(--shadow-lg); } 
        .quote-icon { font-size: 2rem; color: var(--accent-light); margin-bottom: 20px; }
        .client-info { display: flex; align-items: center; gap: 15px; margin-top: 25px; }
        .client-avatar { width: 50px; height: 50px; border-radius: 50%; object-fit: cover; }

        /* Value Props */
        .value-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 40px; }
        .value-card { padding: 40px; background: #fff; border-radius: 20px; border: 1px solid var(--border); transition: box-shadow 0.3s; }
        .value-card:hover { box-shadow: var(--shadow-lg); border-color: var(--primary); }
        .value-icon { font-size: 2.5rem; color: var(--primary); margin-bottom: 25px; transition: transform 0.4s var(--ease-elastic); }
        .value-card:hover .value-icon { transform: scale(1.1) rotate(5deg); }
        .value-card h3 { font-family: var(--font-heading); font-size: 1.5rem; margin-bottom: 15px; }

        /* Featured Projects */
        .featured-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 30px; }
        .featured-item { position: relative; border-radius: 20px; overflow: hidden; height: 500px; }
        .featured-item.large { grid-column: span 2; }
        .featured-img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.8s var(--ease-out); }
        .featured-item:hover .featured-img { transform: scale(1.05); }
        .featured-content { position: absolute; bottom: 0; left: 0; width: 100%; padding: 40px; background: linear-gradient(to top, rgba(15,23,42,0.9), transparent); color: #fff; transform: translateY(20px); opacity: 0; transition: 0.4s; }
        .featured-item:hover .featured-content { transform: translateY(0); opacity: 1; }

        /* Parallax CTA */
        .parallax-cta {
            background-image: linear-gradient(rgba(79, 70, 229, 0.9), rgba(6, 182, 212, 0.9)), url('https://picsum.photos/seed/abstract/1920/600');
            background-attachment: fixed; background-size: cover; background-position: center;
            color: #fff; text-align: center; padding: 120px 0; border-radius: 30px; margin: 60px 0;
        }
        .parallax-cta h2 { font-family: var(--font-heading); font-size: 3.5rem; margin-bottom: 20px; }

        /* Insights/Blog */
        .blog-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); gap: 30px; }
        .blog-card { background: #fff; border-radius: 20px; overflow: hidden; border: 1px solid var(--border); transition: box-shadow 0.3s; }
        .blog-card:hover { box-shadow: var(--shadow-xl); }
        .blog-img { height: 240px; overflow: hidden; }
        .blog-img img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.6s; }
        .blog-card:hover .blog-img img { transform: scale(1.1); }
        .blog-content { padding: 30px; }
        .blog-date { font-size: 0.85rem; color: var(--primary); font-weight: 600; margin-bottom: 10px; display: block; }
        .blog-content h3 { font-family: var(--font-heading); font-size: 1.3rem; margin-bottom: 15px; }

        /* Client Logos */
        .client-logos-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: 40px; align-items: center; justify-items: center; opacity: 0.6; margin-top: 40px; }
        .client-logos-grid i { font-size: 3rem; color: var(--text-main); transition: 0.3s; }
        .client-logos-grid i:hover { color: var(--primary); transform: scale(1.1) rotate(5deg); opacity: 1; }

        /* Other Pages */
        .about-hero { text-align: center; max-width: 800px; margin: 0 auto 80px; }
        .about-hero h1 { font-family: var(--font-heading); font-size: 3.5rem; margin-bottom: 20px; }
        .timeline { position: relative; max-width: 900px; margin: 100px auto; padding-left: 50px; }
        .timeline::before { content: ''; position: absolute; left: 20px; top: 0; bottom: 0; width: 2px; background: var(--border); }
        .timeline-item { position: relative; margin-bottom: 60px; }
        .timeline-dot { position: absolute; left: -39px; top: 5px; width: 16px; height: 16px; background: var(--primary); border-radius: 50%; border: 4px solid #fff; box-shadow: 0 0 0 2px var(--border); }
        
        .service-card { background: #fff; padding: 50px 40px; border-radius: 24px; border: 1px solid var(--border); transition: box-shadow 0.3s; }
        .service-card:hover { border-color: var(--primary); box-shadow: var(--shadow-xl); }
        .service-icon { width: 70px; height: 70px; background: var(--bg-secondary); border-radius: 16px; display: flex; align-items: center; justify-content: center; font-size: 1.8rem; color: var(--primary); margin-bottom: 30px; transition: 0.4s var(--ease-elastic); }
        .service-card:hover .service-icon { background: var(--primary); color: #fff; transform: rotateY(180deg); }
        
        .project-card { border-radius: 24px; overflow: hidden; position: relative; height: 500px; }
        .project-img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.6s cubic-bezier(0.25, 1, 0.5, 1); }
        .project-card:hover .project-img { transform: scale(1.1); }
        .project-overlay { position: absolute; inset: 0; background: linear-gradient(to top, rgba(15, 23, 42, 0.9), transparent); display: flex; flex-direction: column; justify-content: flex-end; padding: 40px; opacity: 0; transform: translateY(20px); transition: all 0.4s; }
        .project-card:hover .project-overlay { opacity: 1; transform: translateY(0); }
        
        .contact-layout { display: grid; grid-template-columns: 1fr 1.5fr; gap: 60px; background: #fff; border-radius: 30px; overflow: hidden; box-shadow: var(--shadow-xl); }
        .contact-info { background: var(--text-main); color: #fff; padding: 80px 60px; position: relative; }
        .contact-form { padding: 80px 60px; }
        .form-group { position: relative; margin-bottom: 30px; }
        .form-control { width: 100%; background: #f8fafc; border: 2px solid transparent; padding: 20px; border-radius: 12px; font-size: 1rem; font-family: var(--font-body); transition: 0.3s; }
        .form-control:focus { outline: none; border-color: var(--primary); background: #fff; }
        .form-label { position: absolute; left: 20px; top: 20px; color: var(--text-muted); pointer-events: none; transition: 0.3s; }
        .form-control:focus ~ .form-label, .form-control:not(:placeholder-shown) ~ .form-label { top: -10px; left: 15px; font-size: 0.8rem; background: #fff; padding: 0 5px; color: var(--primary); }

        /* --- FOOTER --- */
        footer { background: #fff; padding-top: 80px; border-top: 1px solid var(--border); margin-top: 80px; }
        .footer-grid { display: grid; grid-template-columns: 2fr 1fr 1fr 1.5fr; gap: 40px; margin-bottom: 60px; }
        .footer-brand p { color: var(--text-muted); margin-top: 20px; font-size: 0.95rem; max-width: 300px; }
        .footer-col h4 { font-family: var(--font-heading); font-size: 1.1rem; margin-bottom: 25px; color: var(--text-main); }
        .footer-links li { margin-bottom: 12px; }
        .footer-links a { color: var(--text-muted); transition: 0.3s; font-size: 0.95rem; }
        .footer-links a:hover { color: var(--primary); padding-left: 5px; }
        .newsletter-form { position: relative; }
        .newsletter-input { width: 100%; padding: 15px; padding-right: 50px; border-radius: 50px; border: 1px solid var(--border); background: var(--bg-body); outline: none; transition: 0.3s; }
        .newsletter-input:focus { border-color: var(--primary); background: #fff; }
        .newsletter-btn { position: absolute; right: 5px; top: 5px; width: 40px; height: 40px; border-radius: 50%; background: var(--primary); color: #fff; border: none; cursor: none; transition: 0.3s; display: flex; align-items: center; justify-content: center; }
        .newsletter-btn:hover { background: var(--accent); }
        .footer-bottom { border-top: 1px solid var(--border); padding: 30px 0; display: flex; justify-content: space-between; align-items: center; color: var(--text-muted); font-size: 0.9rem; }
        .social-links { display: flex; gap: 20px; }
        .social-links a { color: var(--text-main); font-size: 1.2rem; transition: 0.3s; }
        .social-links a:hover { color: var(--primary); transform: translateY(-3px); }

        .toast { position: fixed; bottom: 40px; right: 40px; background: #fff; padding: 20px 30px; border-radius: 12px; box-shadow: var(--shadow-xl); border-left: 5px solid var(--primary); display: flex; align-items: center; gap: 15px; transform: translateX(200%); transition: 0.5s cubic-bezier(0.68, -0.55, 0.27, 1.55); z-index: 9999; }
        .toast.show { transform: translateX(0); }

        /* --- Responsive --- */
        @media (max-width: 992px) {
            .hero h1 { font-size: 3.5rem; }
            .stats-grid, .testimonial-grid, .contact-layout, .footer-grid, .featured-grid { grid-template-columns: 1fr; }
            .featured-item.large { grid-column: span 1; }
            .footer-bottom { flex-direction: column; gap: 20px; text-align: center; }
        }
        @media (max-width: 768px) {
            .nav-links { position: fixed; top: 0; right: 0; height: 100vh; width: 80%; background: #fff; flex-direction: column; justify-content: center; align-items: center; transform: translateX(100%); transition: 0.4s var(--ease-out); box-shadow: -10px 0 30px rgba(0,0,0,0.1); }
            .nav-links.active { transform: translateX(0); }
            .mobile-toggle { display: block; z-index: 1001; }
            .cursor-dot, .cursor-outline { display: none; }
            * { cursor: auto; }
            .hero h1 { font-size: 2.5rem; }
            .parallax-cta h2 { font-size: 2.5rem; }
        }
    </style>
</head>
<body>

    <!-- Progress Bar -->
    <div class="progress-bar" id="progress-bar"></div>

    <!-- Canvas Background (Global, visible behind content on non-video pages) -->
    <canvas id="canvas-bg"></canvas>

    <!-- Navigation -->
    <header id="header">
        <div class="container nav-content">
            <a href="#" class="logo" onclick="navigateTo('home')">
                <!-- Logo Image with Animation -->
                <img src="assets/Logo_Image.png" alt="Cromora Logo" class="logo-img">
                CROMORA Designs
            </a>
            <div class="mobile-toggle" id="mobile-toggle">
                <i class="fa-solid fa-bars"></i>
            </div>
            <nav>
                <ul class="nav-links" id="nav-links">
                    <li><a class="nav-link active" data-target="home">Home</a></li>
                    <li><a class="nav-link" data-target="about">About</a></li>
                    <li><a class="nav-link" data-target="services">Services</a></li>
                    <li><a class="nav-link" data-target="portfolio">Portfolio</a></li>
                    <li><a class="nav-link" data-target="process">Process</a></li>
                    <li><a class="nav-link" data-target="contact">Contact</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <main id="app">

        <!-- === HOME PAGE === -->
        <section id="home" class="page-section active-page">
            <!-- Hero with Background Video -->
            <div class="hero">
                <!-- Video Background -->
                <div class="hero-video-wrapper">
                    <!-- Using a royalty-free abstract tech video suitable for light theme -->
                    <video autoplay muted loop playsinline>
                        <source src="assets/Hero Video 4.mp4" type="video/mp4">
                        Your browser does not support the video tag.
                    </video>
                    <!-- White Overlay to ensure text contrast -->
                    <div class="hero-overlay"></div>
                </div>

                <!-- Parallax Shapes (Floating on top of video) -->
                <div class="shape shape-1" data-speed="2"></div>
                <div class="shape shape-2" data-speed="-2"></div>
                
                <div class="container">
                    <div class="reveal">
                        <span class="btn btn-outline" style="border-radius: 30px; padding: 8px 20px; font-size: 0.85rem; margin-bottom: 25px; pointer-events: none;">
                            <i class="fa-solid fa-sparkles"></i> Award Winning Agency
                        </span>
                        <!--<h1 class="gradient-text">We craft digital experiences that define the future.</h1>-->
			<h1 style="color: white">We craft digital experiences that define the future.</h1>
                        <p style="color: white; opacity: 0; animation: fadeIn 1s forwards 0.8s;">Cromora Designs is an elite UI/UX studio blending aesthetics with strategy. We transform complex problems into intuitive, beautiful, and high-converting digital products.</p>
                        <div style="display: flex; gap: 20px; opacity: 0; animation: fadeIn 1s forwards 1s;">
                            <button class="btn btn-primary" onclick="navigateTo('portfolio')">Explore Work <i class="fa-solid fa-arrow-right"></i></button>
                            <button class="btn btn-outline" onclick="navigateTo('contact')">Let's Talk</button>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Marquee -->
            <div class="marquee-container reveal">
                <div class="marquee-content">
                    <span class="marquee-item">UI/UX Design • Product Strategy • Frontend Dev • Brand Identity • Motion Design • UI/UX Design • Product Strategy • Frontend Dev • Brand Identity • Motion Design •</span>
                </div>
            </div>

            <!-- Stats Section -->
            <div class="container">
                <div class="stats-grid reveal">
                    <div class="stat-item"><h3 data-target="150">0</h3><p>Projects Shipped</p></div>
                    <div class="stat-item"><h3 data-target="45">0</h3><p>Global Clients</p></div>
                    <div class="stat-item"><h3 data-target="12">0</h3><p>Design Awards</p></div>
                    <div class="stat-item"><h3 data-target="98">0</h3><p>Success Rate %</p></div>
                </div>
            </div>

            <!-- Value Proposition -->
            <div class="container section-padding">
                <div class="section-title-center reveal">
                    <h2 class="split-text">Why Choose Cromora?</h2>
                    <p>We don't just design screens; we engineer business results through thoughtful design.</p>
                </div>
                <div class="value-grid">
                    <div class="value-card tilt-card reveal">
                        <div class="tilt-inner">
                            <i class="fa-solid fa-bullseye value-icon"></i>
                            <h3>User-Centric Approach</h3>
                            <p style="color: var(--text-muted);">Every pixel is placed with purpose. We validate our decisions through rigorous user testing to ensure optimal experiences.</p>
                        </div>
                    </div>
                    <div class="value-card tilt-card reveal">
                        <div class="tilt-inner">
                            <i class="fa-solid fa-bolt value-icon"></i>
                            <h3>Speed & Agility</h3>
                            <p style="color: var(--text-muted);">Our lean design process allows us to iterate quickly without compromising on quality, getting you to market faster.</p>
                        </div>
                    </div>
                    <div class="value-card tilt-card reveal">
                        <div class="tilt-inner">
                            <i class="fa-solid fa-shield-halved value-icon"></i>
                            <h3>Scalable Systems</h3>
                            <p style="color: var(--text-muted);">We build design systems that grow with your product, ensuring consistency as you scale from MVP to Enterprise.</p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Featured Work -->
            <div class="container section-padding" style="background: #fff; border-radius: 40px; padding: 80px;">
                <div class="section-title-center reveal">
                    <h2 class="split-text">Featured Masterpieces</h2>
                    <p>A glimpse into our recent collaborations with industry leaders.</p>
                </div>
                <div class="featured-grid">
                    <div class="featured-item large reveal">
                        <img src="https://picsum.photos/seed/fintech/1200/600" alt="Project" class="featured-img">
                        <div class="featured-content">
                            <h3 style="font-family: var(--font-heading); font-size: 2.5rem;">Nova Finance App</h3>
                            <p style="margin-bottom: 20px;">Reimagining banking for the digital native generation.</p>
                            <button class="btn btn-white" onclick="navigateTo('portfolio')">View Case Study</button>
                        </div>
                    </div>
                    <div class="featured-item reveal">
                        <img src="https://picsum.photos/seed/arch/600/500" alt="Project" class="featured-img">
                        <div class="featured-content">
                            <h3 style="font-family: var(--font-heading); font-size: 1.8rem;">Arch Studio</h3>
                            <p>Minimalist web presence.</p>
                        </div>
                    </div>
                    <div class="featured-item reveal">
                        <img src="https://picsum.photos/seed/coffee/600/500" alt="Project" class="featured-img">
                        <div class="featured-content">
                            <h3 style="font-family: var(--font-heading); font-size: 1.8rem;">Brew Co.</h3>
                            <p>Complete brand overhaul.</p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Trusted By -->
            <div class="container section-padding" style="text-align: center;">
                <h4 style="color: var(--text-muted); text-transform: uppercase; letter-spacing: 2px; margin-bottom: 30px;" class="reveal">Trusted by innovative teams</h4>
                <div class="client-logos-grid reveal">
                    <i class="fa-brands fa-google"></i>
                    <i class="fa-brands fa-amazon"></i>
                    <i class="fa-brands fa-spotify"></i>
                    <i class="fa-brands fa-airbnb"></i>
                    <i class="fa-brands fa-stripe"></i>
                </div>
            </div>

            <!-- Parallax CTA -->
            <div class="container reveal">
                <div class="parallax-cta">
                    <h2 class="split-text">Ready to elevate your brand?</h2>
                    <p style="font-size: 1.2rem; margin-bottom: 30px; opacity: 0.9;">Let's build something extraordinary together.</p>
                    <button class="btn btn-white" onclick="navigateTo('contact')">Start Your Project</button>
                </div>
            </div>

            <!-- Insights -->
            <div class="container section-padding">
                <div class="section-title-center reveal">
                    <h2 class="split-text">Latest Insights</h2>
                    <p>Trends, tutorials, and thoughts from the design team.</p>
                </div>
                <div class="blog-grid">
                    <div class="blog-card tilt-card reveal">
                        <div class="tilt-inner">
                            <div class="blog-img"><img src="https://picsum.photos/seed/blog1/600/400" alt="Blog"></div>
                            <div class="blog-content">
                                <span class="blog-date">Oct 12, 2023</span>
                                <h3>The Future of Minimalist UI Design</h3>
                                <a href="#" style="color: var(--primary); font-weight: 600; font-size: 0.9rem;">Read Article <i class="fa-solid fa-arrow-right"></i></a>
                            </div>
                        </div>
                    </div>
                    <div class="blog-card tilt-card reveal">
                        <div class="tilt-inner">
                            <div class="blog-img"><img src="https://picsum.photos/seed/blog2/600/400" alt="Blog"></div>
                            <div class="blog-content">
                                <span class="blog-date">Sep 28, 2023</span>
                                <h3>Accessibility is not optional</h3>
                                <a href="#" style="color: var(--primary); font-weight: 600; font-size: 0.9rem;">Read Article <i class="fa-solid fa-arrow-right"></i></a>
                            </div>
                        </div>
                    </div>
                    <div class="blog-card tilt-card reveal">
                        <div class="tilt-inner">
                            <div class="blog-img"><img src="https://picsum.photos/seed/blog3/600/400" alt="Blog"></div>
                            <div class="blog-content">
                                <span class="blog-date">Sep 15, 2023</span>
                                <h3>Micro-interactions that matter</h3>
                                <a href="#" style="color: var(--primary); font-weight: 600; font-size: 0.9rem;">Read Article <i class="fa-solid fa-arrow-right"></i></a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Testimonials -->
            <div class="container section-padding">
                <div class="section-header reveal">
                    <h2 style="font-family: var(--font-heading); font-size: 2.5rem;">Client Stories</h2>
                </div>
                <div class="testimonial-grid">
                    <div class="testimonial-card tilt-card reveal">
                        <div class="tilt-inner">
                            <i class="fa-solid fa-quote-left quote-icon"></i>
                            <p style="font-size: 1.1rem; color: var(--text-body);">"Cromora didn't just design our app; they redefined our entire business logic."</p>
                            <div class="client-info">
                                <img src="https://picsum.photos/seed/user1/100/100" class="client-avatar" alt="User">
                                <div><h5 style="font-family: var(--font-heading);">Sarah Jenkins</h5><span style="font-size: 0.85rem; color: var(--text-muted);">CEO, FinTech Nova</span></div>
                            </div>
                        </div>
                    </div>
                    <div class="testimonial-card tilt-card reveal">
                        <div class="tilt-inner">
                            <i class="fa-solid fa-quote-left quote-icon"></i>
                            <p style="font-size: 1.1rem; color: var(--text-body);">"The team's ability to translate abstract concepts into pixel-perfect interfaces is pure magic."</p>
                            <div class="client-info">
                                <img src="https://picsum.photos/seed/user2/100/100" class="client-avatar" alt="User">
                                <div><h5 style="font-family: var(--font-heading);">David Chen</h5><span style="font-size: 0.85rem; color: var(--text-muted);">Product Lead, Orbit</span></div>
                            </div>
                        </div>
                    </div>
                    <div class="testimonial-card tilt-card reveal">
                        <div class="tilt-inner">
                            <i class="fa-solid fa-quote-left quote-icon"></i>
                            <p style="font-size: 1.1rem; color: var(--text-body);">"Professional, timely, and incredibly creative. They delivered a design system that scaled perfectly."</p>
                            <div class="client-info">
                                <img src="https://picsum.photos/seed/user3/100/100" class="client-avatar" alt="User">
                                <div><h5 style="font-family: var(--font-heading);">Elena Rodriguez</h5><span style="font-size: 0.85rem; color: var(--text-muted);">Founder, Luxe</span></div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- === ABOUT PAGE === -->
        <section id="about" class="page-section">
            <div class="container section-padding">
                <div class="about-hero reveal">
                    <span class="gradient-text" style="font-weight: 600; letter-spacing: 2px;">WHO WE ARE</span>
                    <h1>We are a collective of dreamers, doers, and pixel-perfectionists.</h1>
                    <p style="font-size: 1.2rem; color: var(--text-muted); margin-top: 20px;">Cromora was born from a desire to erase the line between art and engineering. We don't just draw screens; we engineer emotions and interactions that leave lasting impressions.</p>
                </div>

                <!-- History Timeline -->
                <div class="timeline">
                    <h3 style="text-align: center; font-family: var(--font-heading); font-size: 2rem; margin-bottom: 60px;" class="reveal">Our Journey</h3>
                    
                    <div class="timeline-item reveal">
                        <span class="timeline-dot"></span>
                        <span class="timeline-date">2018 - Inception</span>
                        <h3 style="font-family: var(--font-heading); font-size: 1.5rem; margin-bottom: 10px;">The Garage Days</h3>
                        <p style="color: var(--text-muted);">Started with two laptops and a vision to make enterprise software look as good as consumer apps.</p>
                    </div>

                    <div class="timeline-item reveal">
                        <span class="timeline-dot"></span>
                        <span class="timeline-date">2020 - Expansion</span>
                        <h3 style="font-family: var(--font-heading); font-size: 1.5rem; margin-bottom: 10px;">Going Global</h3>
                        <p style="color: var(--text-muted);">Expanded our team to include top-tier researchers and developers. Opened our first studio in San Francisco.</p>
                    </div>

                    <div class="timeline-item reveal">
                        <span class="timeline-dot"></span>
                        <span class="timeline-date">2023 - Excellence</span>
                        <h3 style="font-family: var(--font-heading); font-size: 1.5rem; margin-bottom: 10px;">Award Recognition</h3>
                        <p style="color: var(--text-muted);">Recognized by Awwwards and CSS Design Awards. Partnered with Fortune 500 companies.</p>
                    </div>
                </div>

                <!-- Team Grid -->
                <div class="reveal" style="margin-top: 100px;">
                    <h3 style="text-align: center; font-family: var(--font-heading); font-size: 2rem; margin-bottom: 50px;">Meet The Minds</h3>
                    <div class="team-grid">
                        <div class="team-card">
                            <div class="team-img-wrapper">
                                <img src="https://picsum.photos/seed/ceo/400/500" alt="Team Member">
                                <div class="team-social">
                                    <a href="#"><i class="fa-brands fa-linkedin"></i></a>
                                    <a href="#"><i class="fa-brands fa-twitter"></i></a>
                                    <a href="#"><i class="fa-solid fa-envelope"></i></a>
                                </div>
                            </div>
                            <div class="team-info">
                                <h4 style="font-family: var(--font-heading); font-size: 1.2rem;">Alex Morgan</h4>
                                <p style="color: var(--primary); font-size: 0.9rem;">Founder & CEO</p>
                            </div>
                        </div>
                        <div class="team-card">
                            <div class="team-img-wrapper">
                                <img src="https://picsum.photos/seed/des/400/500" alt="Team Member">
                                <div class="team-social">
                                    <a href="#"><i class="fa-brands fa-dribbble"></i></a>
                                    <a href="#"><i class="fa-brands fa-behance"></i></a>
                                </div>
                            </div>
                            <div class="team-info">
                                <h4 style="font-family: var(--font-heading); font-size: 1.2rem;">Jessica Lee</h4>
                                <p style="color: var(--primary); font-size: 0.9rem;">Head of Design</p>
                            </div>
                        </div>
                        <div class="team-card">
                            <div class="team-img-wrapper">
                                <img src="https://picsum.photos/seed/dev/400/500" alt="Team Member">
                                <div class="team-social">
                                    <a href="#"><i class="fa-brands fa-github"></i></a>
                                    <a href="#"><i class="fa-brands fa-linkedin"></i></a>
                                </div>
                            </div>
                            <div class="team-info">
                                <h4 style="font-family: var(--font-heading); font-size: 1.2rem;">Marcus Johnson</h4>
                                <p style="color: var(--primary); font-size: 0.9rem;">Lead Developer</p>
                            </div>
                        </div>
                        <div class="team-card">
                            <div class="team-img-wrapper">
                                <img src="https://picsum.photos/seed/ux/400/500" alt="Team Member">
                                <div class="team-social">
                                    <a href="#"><i class="fa-brands fa-medium"></i></a>
                                    <a href="#"><i class="fa-brands fa-twitter"></i></a>
                                </div>
                            </div>
                            <div class="team-info">
                                <h4 style="font-family: var(--font-heading); font-size: 1.2rem;">Priya Patel</h4>
                                <p style="color: var(--primary); font-size: 0.9rem;">UX Researcher</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>
        <!-- === SERVICES PAGE === -->
        <section id="services" class="page-section">
            <div class="container section-padding">
                <div class="section-header reveal">
                    <span class="gradient-text" style="font-weight: 600;">OUR EXPERTISE</span>
                    <h2 style="font-family: var(--font-heading); font-size: 3rem; margin-top: 10px;">Comprehensive Design Solutions</h2>
                    <p style="max-width: 600px; margin: 20px auto;">From the first sketch to the final pixel, we provide end-to-end design services that cover every aspect of the digital product lifecycle.</p>
                </div>

                <div class="services-grid" style="display: grid; grid-template-columns: repeat(auto-fit, minmax(350px, 1fr)); gap: 30px;">
                    <div class="service-card reveal">
                        <i class="fa-solid fa-layer-group service-icon"></i>
                        <h3 style="font-family: var(--font-heading); font-size: 1.5rem; margin-bottom: 15px;">Product Design</h3>
                        <p style="color: var(--text-muted);">We design complete digital ecosystems. From SaaS platforms to mobile apps, we ensure every interaction is meaningful.</p>
                        <ul class="service-list">
                            <li><i class="fa-solid fa-check"></i> User Flow Architecture</li>
                            <li><i class="fa-solid fa-check"></i> High-Fidelity Prototyping</li>
                            <li><i class="fa-solid fa-check"></i> Interactive Mockups</li>
                        </ul>
                    </div>

                    <div class="service-card reveal reveal-delay-1">
                        <i class="fa-solid fa-wand-magic-sparkles service-icon"></i>
                        <h3 style="font-family: var(--font-heading); font-size: 1.5rem; margin-bottom: 15px;">Brand Identity</h3>
                        <p style="color: var(--text-muted);">A brand is more than a logo. We build visual languages, voice, and personality that resonate with your audience.</p>
                        <ul class="service-list">
                            <li><i class="fa-solid fa-check"></i> Logo & Visual Assets</li>
                            <li><i class="fa-solid fa-check"></i> Brand Guidelines</li>
                            <li><i class="fa-solid fa-check"></i> Marketing Collateral</li>
                        </ul>
                    </div>

                    <div class="service-card reveal reveal-delay-2">
                        <i class="fa-solid fa-code service-icon"></i>
                        <h3 style="font-family: var(--font-heading); font-size: 1.5rem; margin-bottom: 15px;">Frontend Development</h3>
                        <p style="color: var(--text-muted);">We bridge the gap between design and engineering with clean, semantic, and performant code.</p>
                        <ul class="service-list">
                            <li><i class="fa-solid fa-check"></i> React / Vue Development</li>
                            <li><i class="fa-solid fa-check"></i> Responsive Web Design</li>
                            <li><i class="fa-solid fa-check"></i> Animation Integration</li>
                        </ul>
                    </div>
                    
                    <div class="service-card reveal">
                        <i class="fa-solid fa-magnifying-glass-chart service-icon"></i>
                        <h3 style="font-family: var(--font-heading); font-size: 1.5rem; margin-bottom: 15px;">UX Research</h3>
                        <p style="color: var(--text-muted);">Data-driven decisions. We validate assumptions through rigorous testing and user analysis.</p>
                        <ul class="service-list">
                            <li><i class="fa-solid fa-check"></i> Usability Testing</li>
                            <li><i class="fa-solid fa-check"></i> User Personas</li>
                            <li><i class="fa-solid fa-check"></i> Competitor Analysis</li>
                        </ul>
                    </div>

                    <div class="service-card reveal reveal-delay-1">
                        <i class="fa-solid fa-mobile-screen service-icon"></i>
                        <h3 style="font-family: var(--font-heading); font-size: 1.5rem; margin-bottom: 15px;">Mobile App Design</h3>
                        <p style="color: var(--text-muted);">Native experiences designed specifically for iOS and Android human interface guidelines.</p>
                        <ul class="service-list">
                            <li><i class="fa-solid fa-check"></i> iOS & Android Guidelines</li>
                            <li><i class="fa-solid fa-check"></i> Gesture Interactions</li>
                            <li><i class="fa-solid fa-check"></i> App Store Optimization</li>
                        </ul>
                    </div>

                    <div class="service-card reveal reveal-delay-2">
                        <i class="fa-solid fa-swatchbook service-icon"></i>
                        <h3 style="font-family: var(--font-heading); font-size: 1.5rem; margin-bottom: 15px;">Design Systems</h3>
                        <p style="color: var(--text-muted);">Scalable libraries of components that ensure consistency across your entire product suite.</p>
                        <ul class="service-list">
                            <li><i class="fa-solid fa-check"></i> Component Libraries</li>
                            <li><i class="fa-solid fa-check"></i> Token-Based Styling</li>
                            <li><i class="fa-solid fa-check"></i> Documentation (Storybook)</li>
                        </ul>
                    </div>
                </div>
            </div>
        </section>
        <!-- === PORTFOLIO PAGE === -->
        <section id="portfolio" class="page-section">
            <div class="container section-padding">
                <div class="section-header reveal">
                    <h2 class="gradient-text" style="font-family: var(--font-heading); font-size: 3rem;">Selected Works</h2>
                    <p class="gradient-text">A showcase of our best digital products and brand campaigns.</p>
                </div>

                <div class="portfolio-filter reveal">
                    <button class="filter-btn active" data-filter="all">All</button>
                    <button class="filter-btn" data-filter="web">Web Design</button>
                    <button class="filter-btn" data-filter="app">Mobile Apps</button>
                    <button class="filter-btn" data-filter="brand">Branding</button>
                </div>

                <div class="portfolio-grid">
                    <div class="project-card reveal" data-category="app">
                        <div class="project-number">01</div>
                        <img src="https://picsum.photos/seed/finance/600/800" alt="Project" class="project-img">
                        <div class="project-overlay">
                            <h3 style="font-size: 2rem; font-family: var(--font-heading);">Nova Finance</h3>
                            <p style="margin: 10px 0;">A next-gen banking app for Gen Z.</p>
                            <div style="display: flex; gap: 10px; margin-bottom: 20px;">
                                <span style="background: var(--primary); padding: 5px 10px; border-radius: 4px; font-size: 0.8rem;">Fintech</span>
                                <span style="border: 1px solid #fff; padding: 5px 10px; border-radius: 4px; font-size: 0.8rem;">Mobile</span>
                            </div>
                            <a href="#" class="btn btn-primary" style="padding: 10px 25px; font-size: 0.9rem;">View Case Study</a>
                        </div>
                    </div>

                    <div class="project-card reveal reveal-delay-1" data-category="web">
                        <div class="project-number">02</div>
                        <img src="https://picsum.photos/seed/arch/600/800" alt="Project" class="project-img">
                        <div class="project-overlay">
                            <h3 style="font-size: 2rem; font-family: var(--font-heading);">Arch Studio</h3>
                            <p style="margin: 10px 0;">Minimalist portfolio for architects.</p>
                            <div style="display: flex; gap: 10px; margin-bottom: 20px;">
                                <span style="background: var(--primary); padding: 5px 10px; border-radius: 4px; font-size: 0.8rem;">Web</span>
                                <span style="border: 1px solid #fff; padding: 5px 10px; border-radius: 4px; font-size: 0.8rem;">3D</span>
                            </div>
                            <a href="#" class="btn btn-primary" style="padding: 10px 25px; font-size: 0.9rem;">View Case Study</a>
                        </div>
                    </div>

                    <div class="project-card reveal reveal-delay-2" data-category="brand">
                        <div class="project-number">03</div>
                        <img src="https://picsum.photos/seed/coffee/600/800" alt="Project" class="project-img">
                        <div class="project-overlay">
                            <h3 style="font-size: 2rem; font-family: var(--font-heading);">Brew Co.</h3>
                            <p style="margin: 10px 0;">Complete brand identity overhaul.</p>
                            <div style="display: flex; gap: 10px; margin-bottom: 20px;">
                                <span style="background: var(--primary); padding: 5px 10px; border-radius: 4px; font-size: 0.8rem;">Branding</span>
                                <span style="border: 1px solid #fff; padding: 5px 10px; border-radius: 4px; font-size: 0.8rem;">Packaging</span>
                            </div>
                            <a href="#" class="btn btn-primary" style="padding: 10px 25px; font-size: 0.9rem;">View Case Study</a>
                        </div>
                    </div>

                    <div class="project-card reveal" data-category="web">
                        <div class="project-number">04</div>
                        <img src="https://picsum.photos/seed/tech/600/800" alt="Project" class="project-img">
                        <div class="project-overlay">
                            <h3 style="font-size: 2rem; font-family: var(--font-heading);">CyberSecure</h3>
                            <p style="margin: 10px 0;">Enterprise security dashboard.</p>
                            <div style="display: flex; gap: 10px; margin-bottom: 20px;">
                                <span style="background: var(--primary); padding: 5px 10px; border-radius: 4px; font-size: 0.8rem;">SaaS</span>
                                <span style="border: 1px solid #fff; padding: 5px 10px; border-radius: 4px; font-size: 0.8rem;">B2B</span>
                            </div>
                            <a href="#" class="btn btn-primary" style="padding: 10px 25px; font-size: 0.9rem;">View Case Study</a>
                        </div>
                    </div>

                    <div class="project-card reveal" data-category="web">
                        <div class="project-number">04</div>
                        <img src="https://picsum.photos/seed/tech/600/800" alt="Project" class="project-img">
                        <div class="project-overlay">
                            <h3 style="font-size: 2rem; font-family: var(--font-heading);">CyberSecure</h3>
                            <p style="margin: 10px 0;">Enterprise security dashboard.</p>
                            <div style="display: flex; gap: 10px; margin-bottom: 20px;">
                                <span style="background: var(--primary); padding: 5px 10px; border-radius: 4px; font-size: 0.8rem;">SaaS</span>
                                <span style="border: 1px solid #fff; padding: 5px 10px; border-radius: 4px; font-size: 0.8rem;">B2B</span>
                            </div>
                            <a href="#" class="btn btn-primary" style="padding: 10px 25px; font-size: 0.9rem;">View Case Study</a>
                        </div>
                    </div>

                    <div class="project-card reveal" data-category="web">
                        <div class="project-number">04</div>
                        <img src="https://picsum.photos/seed/tech/600/800" alt="Project" class="project-img">
                        <div class="project-overlay">
                            <h3 style="font-size: 2rem; font-family: var(--font-heading);">CyberSecure</h3>
                            <p style="margin: 10px 0;">Enterprise security dashboard.</p>
                            <div style="display: flex; gap: 10px; margin-bottom: 20px;">
                                <span style="background: var(--primary); padding: 5px 10px; border-radius: 4px; font-size: 0.8rem;">SaaS</span>
                                <span style="border: 1px solid #fff; padding: 5px 10px; border-radius: 4px; font-size: 0.8rem;">B2B</span>
                            </div>
                            <a href="#" class="btn btn-primary" style="padding: 10px 25px; font-size: 0.9rem;">View Case Study</a>
                        </div>
                    </div>

                    <div class="project-card reveal" data-category="web">
                        <div class="project-number">04</div>
                        <img src="https://picsum.photos/seed/tech/600/800" alt="Project" class="project-img">
                        <div class="project-overlay">
                            <h3 style="font-size: 2rem; font-family: var(--font-heading);">CyberSecure</h3>
                            <p style="margin: 10px 0;">Enterprise security dashboard.</p>
                            <div style="display: flex; gap: 10px; margin-bottom: 20px;">
                                <span style="background: var(--primary); padding: 5px 10px; border-radius: 4px; font-size: 0.8rem;">SaaS</span>
                                <span style="border: 1px solid #fff; padding: 5px 10px; border-radius: 4px; font-size: 0.8rem;">B2B</span>
                            </div>
                            <a href="#" class="btn btn-primary" style="padding: 10px 25px; font-size: 0.9rem;">View Case Study</a>
                        </div>
                    </div>

                    <div class="project-card reveal" data-category="web">
                        <div class="project-number">04</div>
                        <img src="https://picsum.photos/seed/tech/600/800" alt="Project" class="project-img">
                        <div class="project-overlay">
                            <h3 style="font-size: 2rem; font-family: var(--font-heading);">CyberSecure</h3>
                            <p style="margin: 10px 0;">Enterprise security dashboard.</p>
                            <div style="display: flex; gap: 10px; margin-bottom: 20px;">
                                <span style="background: var(--primary); padding: 5px 10px; border-radius: 4px; font-size: 0.8rem;">SaaS</span>
                                <span style="border: 1px solid #fff; padding: 5px 10px; border-radius: 4px; font-size: 0.8rem;">B2B</span>
                            </div>
                            <a href="#" class="btn btn-primary" style="padding: 10px 25px; font-size: 0.9rem;">View Case Study</a>
                        </div>
                    </div>

                    <div class="project-card reveal" data-category="web">
                        <div class="project-number">04</div>
                        <img src="https://picsum.photos/seed/tech/600/800" alt="Project" class="project-img">
                        <div class="project-overlay">
                            <h3 style="font-size: 2rem; font-family: var(--font-heading);">CyberSecure</h3>
                            <p style="margin: 10px 0;">Enterprise security dashboard.</p>
                            <div style="display: flex; gap: 10px; margin-bottom: 20px;">
                                <span style="background: var(--primary); padding: 5px 10px; border-radius: 4px; font-size: 0.8rem;">SaaS</span>
                                <span style="border: 1px solid #fff; padding: 5px 10px; border-radius: 4px; font-size: 0.8rem;">B2B</span>
                            </div>
                            <a href="#" class="btn btn-primary" style="padding: 10px 25px; font-size: 0.9rem;">View Case Study</a>
                        </div>
                    </div>			
		    
                </div>
            </div>
        </section>

        <!-- === PROCESS PAGE === -->
        <section id="process" class="page-section">
            <div class="container section-padding">
                <div class="section-header reveal">
                    <h2 class="gradient-text" style="font-family: var(--font-heading); font-size: 3rem;">Our Methodology</h2>
                    <p>A proven framework to take ideas from concept to market.</p>
                </div>

                <div class="process-steps">
                    <div class="step reveal">
                        <div class="step-img">
                            <img src="https://picsum.photos/seed/discovery/800/600" alt="Discovery">
                        </div>
                        <div class="step-content">
                            <div class="step-number">01</div>
                            <h3 style="font-family: var(--font-heading); font-size: 2rem; margin-bottom: 15px;">Discovery & Strategy</h3>
                            <p style="color: var(--text-muted); margin-bottom: 20px;">We start by listening. We conduct stakeholder interviews, competitive audits, and market research to fully understand the landscape and your goals.</p>
                            <ul style="color: var(--text-body);">
                                <li><i class="fa-solid fa-check-circle" style="color: var(--accent); margin-right: 10px;"></i> Stakeholder Workshops</li>
                                <li><i class="fa-solid fa-check-circle" style="color: var(--accent); margin-right: 10px;"></i> User Personas</li>
                            </ul>
                        </div>
                    </div>

                    <div class="step reveal">
                        <div class="step-img">
                            <img src="https://picsum.photos/seed/wireframe/800/600" alt="Design">
                        </div>
                        <div class="step-content">
                            <div class="step-number">02</div>
                            <h3 style="font-family: var(--font-heading); font-size: 2rem; margin-bottom: 15px;">Design & Prototyping</h3>
                            <p style="color: var(--text-muted); margin-bottom: 20px;">We translate strategy into visuals. Low-fidelity wireframes turn into high-fidelity designs, followed by interactive prototypes for testing.</p>
                            <ul style="color: var(--text-body);">
                                <li><i class="fa-solid fa-check-circle" style="color: var(--accent); margin-right: 10px;"></i> Information Architecture</li>
                                <li><i class="fa-solid fa-check-circle" style="color: var(--accent); margin-right: 10px;"></i> UI Mockups</li>
                            </ul>
                        </div>
                    </div>

                    <div class="step reveal">
                        <div class="step-img">
                            <img src="https://picsum.photos/seed/code/800/600" alt="Build">
                        </div>
                        <div class="step-content">
                            <div class="step-number">03</div>
                            <h3 style="font-family: var(--font-heading); font-size: 2rem; margin-bottom: 15px;">Development & Handoff</h3>
                            <p style="color: var(--text-muted); margin-bottom: 20px;">Our engineers bring designs to life. We ensure pixel-perfect implementation and provide comprehensive documentation for your team.</p>
                            <ul style="color: var(--text-body);">
                                <li><i class="fa-solid fa-check-circle" style="color: var(--accent); margin-right: 10px;"></i> Frontend Coding</li>
                                <li><i class="fa-solid fa-check-circle" style="color: var(--accent); margin-right: 10px;"></i> Quality Assurance</li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- === CONTACT PAGE === -->
        <section id="contact" class="page-section">
            <div class="container section-padding">
                <div class="contact-layout reveal">
                    <div class="contact-info">
                        <h2 class="gradient-text" style="font-family: var(--font-heading); font-size: 2.5rem; margin-bottom: 30px;">Let's start a project together.</h2>
                        <p style="margin-bottom: 50px; opacity: 0.9;">Have an idea? We'd love to help you bring it to life. Fill out the form or reach out directly.</p>
                        
                        <div style="margin-bottom: 30px;">
                            <h5 style="margin-bottom: 10px; color: var(--accent-light);">EMAIL US</h5>
                            <p style="font-size: 1.1rem;">hello@cromoradesigns.com</p>
                        </div>
                        
                        <div style="margin-bottom: 30px;">
                            <h5 style="margin-bottom: 10px; color: var(--accent-light);">CALL US</h5>
                            <p style="font-size: 1.1rem;">+1 (555) 987-6543</p>
                        </div>

                        <div style="margin-bottom: 50px;">
                            <h5 style="margin-bottom: 10px; color: var(--accent-light);">VISIT US</h5>
                            <p style="font-size: 1.1rem;">123 Innovation Blvd, Suite 400<br>San Francisco, CA 94103</p>
                        </div>

                        <div style="display: flex; gap: 20px;">
                            <a href="#" style="font-size: 1.5rem; transition: 0.3s;"><i class="fa-brands fa-instagram"></i></a>
                            <a href="#" style="font-size: 1.5rem; transition: 0.3s;"><i class="fa-brands fa-linkedin"></i></a>
                            <a href="#" style="font-size: 1.5rem; transition: 0.3s;"><i class="fa-brands fa-dribbble"></i></a>
                        </div>
                    </div>

                    <div class="contact-form">
                        <h3 style="font-family: var(--font-heading); font-size: 2rem; margin-bottom: 30px;">Send a Message</h3>
                        <form id="contactForm">
                            <div class="form-group">
                                <input type="text" class="form-control" id="name" placeholder=" " required>
                                <label for="name" class="form-label">Your Name</label>
                            </div>
                            <div class="form-group">
                                <input type="email" class="form-control" id="email" placeholder=" " required>
                                <label for="email" class="form-label">Your Email</label>
                            </div>
                            <div class="form-group">
                                <select class="form-control" id="service" required style="appearance: none; cursor: pointer;">
                                    <option value="" disabled selected>Select Service</option>
                                    <option value="web">Web Design</option>
                                    <option value="app">App Design</option>
                                    <option value="brand">Branding</option>
                                    <option value="other">Other</option>
                                </select>
                            </div>
                            <div class="form-group">
                                <textarea class="form-control" id="message" rows="4" placeholder=" " required></textarea>
                                <label for="message" class="form-label">Project Details</label>
                            </div>
                            <button type="submit" class="btn btn-primary" style="width: 100%; justify-content: center;">Send Message</button>
                        </form>
                    </div>
                </div>
            </div>
        </section>

    </main>

    <!-- === FOOTER === -->
    <footer>
        <div class="container">
            <div class="footer-grid">
                <div class="footer-brand reveal">
                    <a href="#" class="logo" onclick="navigateTo('home')">
                        <img src="assets/Logo_Image.png" alt="Cromora Logo" class="logo-img">
                        CROMORA
                    </a>
                    <p>Premium UI/UX Design Studio crafting digital experiences that define the future.</p>
                </div>
                <div class="footer-col reveal">
                    <h4>Company</h4>
                    <ul class="footer-links">
                        <li><a href="#" onclick="navigateTo('about')">About Us</a></li>
                        <li><a href="#" onclick="navigateTo('services')">Services</a></li>
                        <li><a href="#" onclick="navigateTo('portfolio')">Portfolio</a></li>
                        <li><a href="#">Careers</a></li>
                    </ul>
                </div>
                <div class="footer-col reveal">
                    <h4>Resources</h4>
                    <ul class="footer-links">
                        <li><a href="#">Blog</a></li>
                        <li><a href="#">Case Studies</a></li>
                        <li><a href="#">Design System</a></li>
                        <li><a href="#">Support</a></li>
                    </ul>
                </div>
                <div class="footer-col reveal">
                    <h4>Stay Updated</h4>
                    <p style="color: var(--text-muted); font-size: 0.9rem; margin-bottom: 20px;">Join our newsletter for design insights.</p>
                    <form class="newsletter-form" onsubmit="event.preventDefault(); alert('Subscribed!');">
                        <input type="email" placeholder="Email address" class="newsletter-input" required>
                        <button type="submit" class="newsletter-btn"><i class="fa-solid fa-arrow-right"></i></button>
                    </form>
                </div>
            </div>
            <div class="footer-bottom">
                <p>&copy; 2023 Cromora Designs. All rights reserved.</p>
                <div class="social-links">
                    <a href="#"><i class="fa-brands fa-linkedin"></i></a>
                    <a href="#"><i class="fa-brands fa-dribbble"></i></a>
                    <a href="#"><i class="fa-brands fa-twitter"></i></a>
                    <a href="#"><i class="fa-brands fa-instagram"></i></a>
                </div>
            </div>
        </div>
    </footer>

    <!-- Toast -->
    <div class="toast" id="toast">
        <i class="fa-solid fa-circle-check" style="color: var(--primary); font-size: 1.5rem;"></i>
        <div>
            <h4 style="font-family: var(--font-heading);">Message Sent!</h4>
            <p style="font-size: 0.9rem; color: var(--text-muted);">We will get back to you shortly.</p>
        </div>
    </div>

    <script>
        // --- Custom Cursor ---
        const cursorDot = document.querySelector('[data-cursor-dot]');
        const cursorOutline = document.querySelector('[data-cursor-outline]');

        window.addEventListener('mousemove', (e) => {
            const posX = e.clientX;
            const posY = e.clientY;

            // Dot follows instantly
            cursorDot.style.left = `${posX}px`;
            cursorDot.style.top = `${posY}px`;

            // Outline follows with delay (animation handled by CSS transition)
            cursorOutline.animate({
                left: `${posX}px`,
                top: `${posY}px`
            }, { duration: 500, fill: "forwards" });
        });

        // Hover effect for interactive elements
        const interactives = document.querySelectorAll('a, button, input, textarea, .project-card, .team-card');
        interactives.forEach(el => {
            el.addEventListener('mouseenter', () => cursorOutline.classList.add('hovered'));
            el.addEventListener('mouseleave', () => cursorOutline.classList.remove('hovered'));
        });

        // --- Navigation & SPA ---
        const navLinks = document.querySelectorAll('.nav-link');
        const pages = document.querySelectorAll('.page-section');
        const mobileToggle = document.getElementById('mobile-toggle');
        const navList = document.getElementById('nav-links');

        function navigateTo(pageId) {
            navLinks.forEach(link => link.classList.remove('active'));
            const activeLink = document.querySelector(`.nav-link[data-target="${pageId}"]`);
            if(activeLink) activeLink.classList.add('active');

            pages.forEach(page => page.classList.remove('active-page'));
            const target = document.getElementById(pageId);
            if(target) {
                target.classList.add('active-page');
                window.scrollTo(0,0);
                checkScroll(); // Re-trigger animations
                runCounters(); // Re-trigger counters if on Home
            }
            
            navList.classList.remove('active');
        }

        navLinks.forEach(link => link.addEventListener('click', (e) => {
            e.preventDefault();
            navigateTo(link.dataset.target);
        }));

        mobileToggle.addEventListener('click', () => {
            navList.classList.toggle('active');
            mobileToggle.querySelector('i').classList.toggle('fa-xmark');
            mobileToggle.querySelector('i').classList.toggle('fa-bars');
        });

        // --- Scroll Progress & Header Style ---
        window.addEventListener('scroll', () => {
            const scrollTop = document.documentElement.scrollTop;
            const scrollHeight = document.documentElement.scrollHeight - document.documentElement.clientHeight;
            const scrolled = (scrollTop / scrollHeight) * 100;
            document.getElementById('progress-bar').style.width = scrolled + '%';

            const header = document.getElementById('header');
            if(scrollTop > 50) header.classList.add('scrolled');
            else header.classList.remove('scrolled');
        });

        // --- Scroll Reveal Animation ---
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if(entry.isIntersecting) {
                    entry.target.classList.add('active');
                }
            });
        }, { threshold: 0.1 });

        function checkScroll() {
            document.querySelectorAll('.reveal').forEach(el => observer.observe(el));
        }
        checkScroll();

        // --- Number Counter Animation ---
        let counted = false;
        function runCounters() {
            const statsSection = document.querySelector('.stats-grid');
            if(!statsSection) return;
            
            const sectionPos = statsSection.getBoundingClientRect().top;
            const screenPos = window.innerHeight / 1.3;

            if(sectionPos < screenPos && !counted) {
                const counters = document.querySelectorAll('.stat-item h3');
                counters.forEach(counter => {
                    const target = +counter.getAttribute('data-target');
                    const duration = 2000;
                    const increment = target / (duration / 16);
                    
                    let current = 0;
                    const updateCounter = () => {
                        current += increment;
                        if(current < target) {
                            counter.innerText = Math.ceil(current) + "+";
                            requestAnimationFrame(updateCounter);
                        } else {
                            counter.innerText = target + "+";
                        }
                    };
                    updateCounter();
                });
                counted = true;
            }
        }
        window.addEventListener('scroll', runCounters);

        // --- Portfolio Filtering ---
        const filterBtns = document.querySelectorAll('.filter-btn');
        const projectCards = document.querySelectorAll('.project-card');

        filterBtns.forEach(btn => {
            btn.addEventListener('click', () => {
                // Remove active class from all
                filterBtns.forEach(b => b.classList.remove('active'));
                btn.classList.add('active');

                const filter = btn.getAttribute('data-filter');

                projectCards.forEach(card => {
                    if(filter === 'all' || card.getAttribute('data-category') === filter) {
                        card.style.display = 'block';
                        setTimeout(() => card.style.opacity = '1', 10);
                    } else {
                        card.style.opacity = '0';
                        setTimeout(() => card.style.display = 'none', 300);
                    }
                });
            });
        });

        // --- Contact Form ---
        document.getElementById('contactForm').addEventListener('submit', (e) => {
            e.preventDefault();
            const btn = e.target.querySelector('button');
            btn.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i> Sending...';
            
            setTimeout(() => {
                btn.innerHTML = 'Send Message';
                e.target.reset();
                document.getElementById('toast').classList.add('show');
                setTimeout(() => document.getElementById('toast').classList.remove('show'), 3000);
            }, 1500);
        });

        // --- Canvas Animation (Subtle Network) ---
        const canvas = document.getElementById('canvas-bg');
        const ctx = canvas.getContext('2d');
        let particles = [];
        const particleCount = 40;
        let width, height;

        function resize() {
            width = canvas.width = window.innerWidth;
            height = canvas.height = window.innerHeight;
        }

        class Particle {
            constructor() {
                this.x = Math.random() * width;
                this.y = Math.random() * height;
                this.vx = (Math.random() - 0.5) * 0.3;
                this.vy = (Math.random() - 0.5) * 0.3;
                this.size = Math.random() * 2 + 1;
            }
            update() {
                this.x += this.vx; this.y += this.vy;
                if(this.x < 0 || this.x > width) this.vx *= -1;
                if(this.y < 0 || this.y > height) this.vy *= -1;
            }
            draw() {
                ctx.beginPath();
                ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
                ctx.fillStyle = '#cbd5e1';
                ctx.fill();
            }
        }

        function initParticles() {
            particles = [];
            for(let i=0; i<particleCount; i++) particles.push(new Particle());
        }

        function animate() {
            ctx.clearRect(0, 0, width, height);
            particles.forEach((p, i) => {
                p.update();
                p.draw();
                for(let j=i+1; j<particles.length; j++) {
                    const p2 = particles[j];
                    const dist = Math.hypot(p.x - p2.x, p.y - p2.y);
                    if(dist < 150) {
                        ctx.beginPath();
                        ctx.strokeStyle = `rgba(203, 213, 225, ${0.2 - dist/750})`;
                        ctx.lineWidth = 1;
                        ctx.moveTo(p.x, p.y);
                        ctx.lineTo(p2.x, p2.y);
                        ctx.stroke();
                    }
                }
            });
            requestAnimationFrame(animate);
        }

        window.addEventListener('resize', () => { resize(); initParticles(); });
        resize();
        initParticles();
        animate();

    </script>
    <!-- Toast -->
    <div class="toast" id="toast">
        <i class="fa-solid fa-circle-check" style="color: var(--primary); font-size: 1.5rem;"></i>
        <div>
            <h4 style="font-family: var(--font-heading);">Message Sent!</h4>
            <p style="font-size: 0.9rem; color: var(--text-muted);">We will get back to you shortly.</p>
        </div>
    </div>

    <script>
        // --- 2. Navigation & SPA ---
        const navLinks = document.querySelectorAll('.nav-link');
        const pages = document.querySelectorAll('.page-section');
        const mobileToggle = document.getElementById('mobile-toggle');
        const navList = document.getElementById('nav-links');

        function navigateTo(pageId) {
            navLinks.forEach(link => link.classList.remove('active'));
            const activeLink = document.querySelector(`.nav-link[data-target="${pageId}"]`);
            if(activeLink) activeLink.classList.add('active');
            pages.forEach(page => page.classList.remove('active-page'));
            const target = document.getElementById(pageId);
            if(target) {
                target.classList.add('active-page');
                window.scrollTo(0,0);
                checkScroll();
                runCounters();
                runTextReveal();
            }
            navList.classList.remove('active');
        }

        navLinks.forEach(link => link.addEventListener('click', (e) => { e.preventDefault(); navigateTo(link.dataset.target); }));
        mobileToggle.addEventListener('click', () => {
            navList.classList.toggle('active');
            mobileToggle.querySelector('i').classList.toggle('fa-xmark');
            mobileToggle.querySelector('i').classList.toggle('fa-bars');
        });

        // --- 3. Scroll Progress & Header ---
        window.addEventListener('scroll', () => {
            const scrollTop = document.documentElement.scrollTop;
            const scrollHeight = document.documentElement.scrollHeight - document.documentElement.clientHeight;
            const scrolled = (scrollTop / scrollHeight) * 100;
            document.getElementById('progress-bar').style.width = scrolled + '%';
            const header = document.getElementById('header');
            if(scrollTop > 50) header.classList.add('scrolled'); else header.classList.remove('scrolled');
        });

        // --- 4. Scroll Reveal Animation ---
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => { if(entry.isIntersecting) entry.target.classList.add('active'); });
        }, { threshold: 0.1 });
        function checkScroll() { document.querySelectorAll('.reveal').forEach(el => observer.observe(el)); }
        checkScroll();

        // --- 5. Text Split Animation ---
        function runTextReveal() {
            const texts = document.querySelectorAll('.split-text');
            texts.forEach(text => {
                text.innerHTML = text.textContent.replace(/\S/g, "<span>$&</span>");
                setTimeout(() => {
                    const spans = text.querySelectorAll('span');
                    spans.forEach((span, index) => {
                        setTimeout(() => span.classList.add('visible'), index * 30);
                    });
                }, 100);
            });
        }
        runTextReveal();

        // --- 6. 3D Tilt Effect ---
        function initTilt() {
            if (window.matchMedia("(hover: none)").matches) return;
            const cards = document.querySelectorAll('.tilt-card');
            cards.forEach(card => {
                card.addEventListener('mousemove', (e) => {
                    const rect = card.getBoundingClientRect();
                    const x = e.clientX - rect.left; const y = e.clientY - rect.top;
                    const centerX = rect.width / 2; const centerY = rect.height / 2;
                    const rotateX = ((y - centerY) / centerY) * -10; const rotateY = ((x - centerX) / centerX) * 10;
                    card.style.transform = `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) scale3d(1.02, 1.02, 1.02)`;
                });
                card.addEventListener('mouseleave', () => { card.style.transform = 'perspective(1000px) rotateX(0) rotateY(0) scale3d(1, 1, 1)'; });
            });
        }
        initTilt();

        // --- 7. Magnetic Buttons ---
        function initMagnetic() {
            if (window.matchMedia("(hover: none)").matches) return;
            const btns = document.querySelectorAll('.btn');
            btns.forEach(btn => {
                btn.addEventListener('mousemove', (e) => {
                    const rect = btn.getBoundingClientRect();
                    const x = e.clientX - rect.left - rect.width / 2;
                    const y = e.clientY - rect.top - rect.height / 2;
                    btn.style.transform = `translate(${x * 0.3}px, ${y * 0.5}px)`;
                });
                btn.addEventListener('mouseleave', () => { btn.style.transform = 'translate(0, 0)'; });
            });
        }
        initMagnetic();

        // --- 8. Mouse Parallax on Shapes ---
        const shapes = document.querySelectorAll('.shape');
        document.addEventListener('mousemove', (e) => {
            const x = (window.innerWidth - e.pageX * 2) / 100;
            const y = (window.innerHeight - e.pageY * 2) / 100;
            shapes.forEach(shape => {
                const speed = shape.getAttribute('data-speed');
                shape.style.transform = `translate(${x * speed}px, ${y * speed}px)`;
            });
        });

        // --- 9. Number Counters ---
        let counted = false;
        function runCounters() {
            const statsSection = document.querySelector('.stats-grid');
            if(!statsSection) return;
            const sectionPos = statsSection.getBoundingClientRect().top;
            const screenPos = window.innerHeight / 1.3;
            if(sectionPos < screenPos && !counted) {
                const counters = document.querySelectorAll('.stat-item h3');
                counters.forEach(counter => {
                    const target = +counter.getAttribute('data-target');
                    const duration = 2000; const increment = target / (duration / 16);
                    let current = 0;
                    const updateCounter = () => {
                        current += increment;
                        if(current < target) { counter.innerText = Math.ceil(current) + "+"; requestAnimationFrame(updateCounter); } 
                        else { counter.innerText = target + "+"; }
                    };
                    updateCounter();
                });
                counted = true;
            }
        }
        window.addEventListener('scroll', runCounters);

        // --- 10. Contact Form ---
        document.getElementById('contactForm').addEventListener('submit', (e) => {
            e.preventDefault();
            const btn = e.target.querySelector('button');
            btn.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i> Sending...';
            setTimeout(() => { btn.innerHTML = 'Send Message'; e.target.reset(); document.getElementById('toast').classList.add('show'); setTimeout(() => document.getElementById('toast').classList.remove('show'), 3000); }, 1500);
        });

        // --- 11. Canvas Animation ---
        const canvas = document.getElementById('canvas-bg'); const ctx = canvas.getContext('2d');
        let particles = []; const particleCount = 40; let width, height;
        function resize() { width = canvas.width = window.innerWidth; height = canvas.height = window.innerHeight; }
        class Particle {
            constructor() {
                this.x = Math.random() * width; this.y = Math.random() * height;
                this.vx = (Math.random() - 0.5) * 0.3; this.vy = (Math.random() - 0.5) * 0.3;
                this.size = Math.random() * 2 + 1;
            }
            update() { this.x += this.vx; this.y += this.vy; if(this.x < 0 || this.x > width) this.vx *= -1; if(this.y < 0 || this.y > height) this.vy *= -1; }
            draw() { ctx.beginPath(); ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2); ctx.fillStyle = '#cbd5e1'; ctx.fill(); }
        }
        function initParticles() { particles = []; for(let i=0; i<particleCount; i++) particles.push(new Particle()); }
        function animate() {
            ctx.clearRect(0, 0, width, height);
            particles.forEach((p, i) => {
                p.update(); p.draw();
                for(let j=i+1; j<particles.length; j++) {
                    const p2 = particles[j]; const dist = Math.hypot(p.x - p2.x, p.y - p2.y);
                    if(dist < 150) {
                        ctx.beginPath(); ctx.strokeStyle = `rgba(203, 213, 225, ${0.2 - dist/750})`; ctx.lineWidth = 1;
                        ctx.moveTo(p.x, p.y); ctx.lineTo(p2.x, p2.y); ctx.stroke();
                    }
                }
            });
            requestAnimationFrame(animate);
        }
        window.addEventListener('resize', () => { resize(); initParticles(); });
        resize(); initParticles(); animate();
    </script>
</body>
</html>